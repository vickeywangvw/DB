##a miniature relational database:

  


How to Run the Program:
=======================
To execute the operation in the file, we input the address of the operation(command) txt file:
"Sentlist = openfile('G:\inputFile.txt')";



And we can read files like:
==========================
R:=inoutfromfile(sales1)/;
R:=inoutfromfile(sales1)////comments
R:=inoutfromfile(sales1)


funcation the program:
=======================
# input from file
# select
# project
# join
# concat
# sort
# aggregate: * count * sum * avg * countgroup * sumgroup * avggroup * moving sum
# index * hash * btree


